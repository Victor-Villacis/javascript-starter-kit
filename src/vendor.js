/*
 * References to vendor libraries here.
 * This is a good place to add your own vendor libraries.
 * You can also add them to the `vendor` entry in webpack.config.dev.js.
 * When you add them here, they will be automatically added to the vendor bundle.
 * They will be used by webpack in production build and bundled separately in vendor.js.
 * All libraries in this vile will be written to vendor.js., which will be cached until change.
 * This allows users to only download the vendor bundle when it changes.
 * Add all imports used below
 */

import fetch from 'whatwg-fetch'; //eslint-disable-line
